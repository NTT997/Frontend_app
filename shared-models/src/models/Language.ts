/**
 * Represents a readable language with code and identifier.
 * Used for UI localization or default language settings.
 */
export interface ReadableLanguage {
  id: number;
  code: string; // e.g., "en", "fr", "de"
}
